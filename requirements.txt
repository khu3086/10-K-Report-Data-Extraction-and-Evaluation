# Core
openai>=1.40         # used against OpenRouter's OpenAI-compatible API
pydantic>=2.0
pandas>=2.0
PyYAML>=6.0

# PDF parsing
pdfplumber>=0.11.0
PyMuPDF>=1.24.0

# EDGAR fetch + HTML->PDF rendering
requests>=2.31.0
playwright>=1.44.0   # run `playwright install chromium` after pip install

# Dashboard
streamlit>=1.36.0
plotly>=5.22.0
